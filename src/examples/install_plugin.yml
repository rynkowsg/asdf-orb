description: |
  Installs ASDF.

usage:
  version: 2.1

  orbs:
    checkout-orb: rynkowsg/asdf@0.1.0

  jobs:
    test:
      docker: [{image: "cimg/base:stable"}]
      steps:
        - asdf/install
        - asdf/install_plugin: {name: circleci-cli}

  workflows:
    main-workflow:
      jobs:
        - test
